spring:
  application:
    name: Banking
  datasource:
    url: "${DB_URL:jdbc:mysql://146.148.103.183:3306/dev_db?connectTimeout=10000&socketTimeout=30000&tcpKeepAlive=true}"
    username: ${DB_USERNAME:usr_dev}
    password: ${DB_PASSWORD:Dev#Pass2026$}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: 10000
      validation-timeout: 5000
      maximum-pool-size: 10
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

# Placeholder AWS settings for local development.
# Replace these values with the real AWS region, credentials, topic ARN, and queue URL in non-local environments.

