spring:
  application:
    name: Banking
  datasource:
    url: ${DB_URL:jdbc:mysql://146.148.103.183:3306/dev_db}
    username: ${DB_USERNAME:usr_dev}
    password: ${DB_PASSWORD:Dev#Pass2026$}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

# Placeholder AWS settings for local development.
# Replace these values with the real AWS region, credentials, topic ARN, and queue URL in non-local environments.
aws:
  access-key:
  secret-key:
  region: us-east-2
  topic-arn: arn:aws:sns:us-east-2:084845891305:fan_out_010099_to_fedwire.fifo
  pacs002-queue-url: https://sqs.us-east-2.amazonaws.com/084845891305/010099_out_fedwire.fifo
